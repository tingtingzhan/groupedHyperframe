
# ?spatstat.geom::subset.hyperframe
# square-bracket ?spatstat.geom::`[.hyperframe` does *not* drop attributes!!!

# therefore, we can simply use ?spatstat.geom::subset.hyperframe on [groupedHyperframe]
# i.e., no need to write [subset.groupedHyperframe()]